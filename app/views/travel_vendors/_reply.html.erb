<%= form_for @reply, url: {action: "create_reply"}, remote: true do |f| %>
<p> Comment: <%= f.text_area :comment %></p>
<%= hidden_field_tag "id", @comment.id %>
<p> <%= f.submit "comment" %></p>
<% end %>